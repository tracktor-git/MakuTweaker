<Page x:Class="MakuTweakerNew.Perf"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MakuTweakerNew"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      mc:Ignorable="d" 
      d:DesignHeight="672"
      Title="Performance">

    <StackPanel Margin="24,11,24,0">
        <TextBlock x:Name="label" HorizontalAlignment="Left" TextWrapping="Wrap" Text="Performance" FontSize="32" FontFamily="Segoe UI Semibold" Margin="0,0,0,10"/>
        <TextBlock x:Name="infolabel" HorizontalAlignment="Left" TextWrapping="Wrap" Text="TEXT" FontSize="22" FontFamily="Segoe UI Semilight" Margin="0,0,70,15"/>

        <WrapPanel Margin="0,0,0,0">
            <controls:Button x:Name="apply" Content="Apply_Percent" Height="33" Width="228" Click="apply_Click" Margin="0,0,8,4"/>
            <controls:Button x:Name="minpercent" Content="Min_Percent" Height="33" Width="228" Click="minpercent_Click" Margin="0,0,8,4"/>
            <controls:Button x:Name="maxpercent" Content="Max_Percent" Height="33" Width="228" Click="maxpercent_Click" Margin="0,0,8,4"/>
        </WrapPanel>

        <Grid Margin="0,0,0,0" HorizontalAlignment="Left">
            <Slider x:Name="percentslider" Width="700" Margin="0,0,0,0" Minimum="1" Maximum="10" TickPlacement="BottomRight" TickFrequency="1" IsSnapToTickEnabled="True" Height="72" VerticalAlignment="Top" Value="6" AutoToolTipPlacement="None" HorizontalAlignment="Left"/>

            <UniformGrid Columns="10" Width="710" Margin="0,32,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">
                <TextBlock Text="10%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="-2,0,0,0"/>
                <TextBlock Text="20%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="2,0,0,0"/>
                <TextBlock Text="30%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="9,0,0,0"/>
                <TextBlock Text="40%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="12,0,0,0"/>
                <TextBlock Text="50%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="18,0,0,0"/>
                <TextBlock Text="60%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="23,0,0,0"/>
                <TextBlock Text="70%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="28,0,0,0"/>
                <TextBlock Text="80%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="33,0,0,0"/>
                <TextBlock Text="90%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="37,0,0,0" Width="30"/>
                <TextBlock Text="100%" HorizontalAlignment="Left" FontSize="14" FontFamily="Segoe UI" Margin="29,0,0,0" Width="38"/>
            </UniformGrid>
        </Grid>

        <StackPanel Margin="0,10,0,0" HorizontalAlignment="Left">

            <ui:ToggleSwitch x:Name="sleeptimeout" Header="Disable Sleep Timeout" FontSize="18" Toggled="sleeptimeout_Toggled" VerticalAlignment="Top" Margin="0,0,0,0"/>

            <StackPanel Margin="0,10,0,0" Visibility="Collapsed" >
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Header, ElementName=ultperf}" FontSize="18" VerticalAlignment="Center"/>
                    <controls:Button Content="?" Width="18" Height="18" Margin="8,0,0,0" FontSize="12" Padding="0" VerticalAlignment="Center" ToolTipService.InitialShowDelay="100">
                        <controls:Button.ToolTip>
                            <ToolTip MaxWidth="500" x:Name="sys_tooltip_ultperf"/>
                        </controls:Button.ToolTip>
                    </controls:Button>
                </StackPanel>
                <ui:ToggleSwitch x:Name="ultperf" FontSize="18" Header="Enable Ultimate Performance Power Plan" Toggled="ultperf_Toggled">
                    <ui:ToggleSwitch.HeaderTemplate>
                        <DataTemplate />
                    </ui:ToggleSwitch.HeaderTemplate>
                </ui:ToggleSwitch>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>